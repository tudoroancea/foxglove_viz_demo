[project]
name = "foxglove-viz-demo"
version = "0.1.0"
description = "Foxglove WebSocket demo with rich visualization streams"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "foxglove-websocket>=0.1.0",
]
